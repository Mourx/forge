Have to hard-enable UseSimulation, as I cannot find the setting in the menu.
Gane crashed when running in this mode - fluke?
update to not spam print -> each upkeep check boardstate value: improved?
reinforced card scores if boardstate improved: presumably ok if improvement observed.

spellabilitypicker for the AI, swap in the NN there;
  skip Abilities for now, only focus cards?

Get breakpoints working, doesn't seem to run the above function? but must do in a thread somewhere


Work out tensorflow to java
